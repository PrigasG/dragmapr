# Keep only large demo videos in LFS. Package and pkgdown assets must remain
# ordinary Git blobs so GitHub source archives contain usable files.
pkgdown/assets/*.mp4 filter=lfs diff=lfs merge=lfs -text
