# Summarise CRS meaning for dragmapr

Returns plain-language CRS metadata and what the units mean for drag
offsets.

## Usage

``` r
summarise_spatial_crs(x)
```

## Arguments

- x:

  An `sf` object.

## Value

A list with CRS id, name, type, units, bounds, and message.
