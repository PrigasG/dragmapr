% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchy.R
\name{inherit_layout}
\alias{inherit_layout}
\title{Inherit region offsets from a coarser to a finer grouping column}
\usage{
inherit_layout(x, from, to, parent_offsets)
}
\arguments{
\item{x}{An \code{sf} object or data frame containing both \code{from} and \code{to}
columns.}

\item{from}{A character vector of one or more column names defining the
coarser (parent) grouping. The \code{region} values in \code{parent_offsets} must
match the keys produced by \code{make_hierarchy_key(x, from)}.}

\item{to}{A character vector of one or more column names defining the finer
(child) grouping. The \code{region} column of the returned data frame will
contain the keys produced by \code{make_hierarchy_key(x, to)}.}

\item{parent_offsets}{A data frame with \code{region}, \code{dx_m}, and \code{dy_m}
columns keyed to the \code{from} grouping, as returned by \code{\link[=read_offsets]{read_offsets()}} or
the Spatial Studio CSV export. Rows whose \code{region} does not match any
parent key in \code{x} are silently ignored.}
}
\value{
A data frame with \code{region}, \code{dx_m}, and \code{dy_m} columns. Rows whose
parent has no offset entry receive zero movement.
}
\description{
When switching from a parent grouping (e.g., county) to a child grouping
(e.g., municipality), \code{inherit_layout()} maps the parent's drag offsets to
every child region that belongs to each parent. Each child inherits the mean
offset of the parent group it belongs to.
}
\details{
When child names repeat across parents (e.g., "Fairfield" exists in both
Essex and Morris counties), pass both columns in \code{to} so that composite keys
are used: \code{to = c("county", "mun")}. The resulting \code{region} values will be
composite strings like \code{"county=Essex | mun=Fairfield"}, which can be passed
directly to \code{drag_map_prototype()} or \code{render_dragged_map()} via
\code{make_hierarchy_key()}.
}
\examples{
poly <- sf::st_sf(
  county = c("Essex", "Essex", "Morris", "Morris"),
  mun    = c("Fairfield", "Caldwell", "Fairfield", "Roxbury"),
  geometry = sf::st_sfc(
    sf::st_polygon(list(rbind(c(0,1e5),c(1e5,1e5),c(1e5,2e5),c(0,2e5),c(0,1e5)))),
    sf::st_polygon(list(rbind(c(0,0),c(1e5,0),c(1e5,1e5),c(0,1e5),c(0,0)))),
    sf::st_polygon(list(rbind(c(1e5,1e5),c(2e5,1e5),c(2e5,2e5),c(1e5,2e5),c(1e5,1e5)))),
    sf::st_polygon(list(rbind(c(1e5,0),c(2e5,0),c(2e5,1e5),c(1e5,1e5),c(1e5,0)))),
    crs = 3857
  )
)
county_offsets <- data.frame(
  region = c("Essex", "Morris"),
  dx_m   = c(50000, -50000),
  dy_m   = c(0, 0)
)
# "Fairfield" repeats, so use composite to= key
mun_offsets <- inherit_layout(
  poly,
  from           = "county",
  to             = c("county", "mun"),
  parent_offsets = county_offsets
)
mun_offsets
}
\seealso{
\code{\link[=make_hierarchy_key]{make_hierarchy_key()}} for composite key construction,
\code{\link[=create_layout_snapshot]{create_layout_snapshot()}} for capturing and restoring layouts.
}
