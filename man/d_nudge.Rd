% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edit.R
\name{d_nudge}
\alias{d_nudge}
\title{Nudge a region or label offset}
\usage{
d_nudge(state, id, dx_m = 0, dy_m = 0, target = c("region", "label"))
}
\arguments{
\item{state}{A \code{dragmapr_state} object.}

\item{id}{Region id (when \code{target = "region"}) or label id (when
\code{target = "label"}) to nudge.}

\item{dx_m, dy_m}{Distances in metres added to the current offset. Each must
be a single finite number.}

\item{target}{One of \code{"region"} (default) or \code{"label"}.}
}
\value{
An updated \code{dragmapr_state} with a bumped version.
}
\description{
Adds a small increment to the current offset of one region or label in a
\code{\link[=d_state]{d_state()}}. This is the scripted equivalent of dragging slightly in the
browser: existing movement is preserved and the delta is added on top.
Unknown ids gain a new row, matching \code{\link[=update_region_offset]{update_region_offset()}}.
}
\examples{
state <- d_state(
  region_offsets = data.frame(region = "A", dx_m = 10, dy_m = 0)
)
state <- d_nudge(state, "A", dx_m = 5)
state$region_offsets
}
