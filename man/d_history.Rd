% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/history.R
\name{d_history}
\alias{d_history}
\alias{d_history_push}
\alias{d_undo}
\alias{d_redo}
\title{Undo/redo history for dragmapr states}
\usage{
d_history()

d_history_push(history, state)

d_undo(history)

d_redo(history)
}
\arguments{
\item{history}{A \code{dragmapr_history} object created by \code{\link[=d_history]{d_history()}}.}

\item{state}{A \code{dragmapr_state} checkpoint to remember.}
}
\value{
\code{d_history()} returns a \code{dragmapr_history} object;
\code{d_history_push()} returns the history invisibly; \code{d_undo()} and
\code{d_redo()} return the restored \code{dragmapr_state}.
}
\description{
\code{d_history()} creates an undo/redo stack for \code{dragmapr_state} objects. Push
a checkpoint with \code{\link[=d_history]{d_history_push()}} after each meaningful edit, then step
backwards with \code{\link[=d_history]{d_undo()}} and forwards with \code{\link[=d_history]{d_redo()}}. Pushing a new
checkpoint clears the redo stack, matching familiar editor behaviour.
}
\details{
The history object has reference semantics: \code{d_history_push()}, \code{d_undo()},
and \code{d_redo()} modify it in place, so reassignment is optional.
}
\examples{
h <- d_history()
s0 <- d_state(
  region_offsets = data.frame(region = "A", dx_m = 0, dy_m = 0)
)
d_history_push(h, s0)
s1 <- d_nudge(s0, "A", dx_m = 50)
d_history_push(h, s1)
back <- d_undo(h) # s0 offsets again
fwd <- d_redo(h)  # s1 offsets again
d_state_equal(back, s0)
d_state_equal(fwd, s1)
}
