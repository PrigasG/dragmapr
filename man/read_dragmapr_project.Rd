% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{read_dragmapr_project}
\alias{read_dragmapr_project}
\title{Read a dragmapr project bundle}
\usage{
read_dragmapr_project(project)
}
\arguments{
\item{project}{Path to a \code{dragmapr-project.zip} file or an extracted project
directory created by Spatial Studio.}
}
\value{
A named list with elements:
\describe{
\item{\code{source}}{The source \code{sf} object read from \code{source.gpkg}.}
\item{\code{region_offsets}}{Data frame from \code{drag_region_offsets.csv}, or
\code{NULL} if not present.}
\item{\code{label_offsets}}{Data frame from \code{drag_label_offsets.csv}, or
\code{NULL} if not present.}
\item{\code{labels}}{Label table from \code{labels.csv} (as returned by
\code{\link[=as_drag_labels]{as_drag_labels()}}), or \code{NULL}.}
\item{\code{region_palette}}{Named character vector of colors from
\code{palette.csv}, or \code{NULL}.}
\item{\code{metadata}}{Named list parsed from \code{metadata.json}.}
\item{\code{path}}{Path to the extracted project directory.}
}
}
\description{
Reads a Spatial Studio project ZIP or extracted project directory and
returns its components as a named list. This is the low-level companion to
\code{\link[=render_dragmapr_project]{render_dragmapr_project()}}: use it when you want to access the raw source
geometry, offsets, labels, or palette programmatically before rendering.
}
\examples{
if(interactive()){
bundle <- read_dragmapr_project("dragmapr-project.zip")
names(bundle)
nrow(bundle$source)
}
}
\seealso{
\code{\link[=render_dragmapr_project]{render_dragmapr_project()}} to render a project bundle directly;
\code{\link[=write_dragmapr_project]{write_dragmapr_project()}} to create a project bundle from R objects.
}
