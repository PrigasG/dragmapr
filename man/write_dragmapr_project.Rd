% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{write_dragmapr_project}
\alias{write_dragmapr_project}
\title{Write a dragmapr project bundle}
\usage{
write_dragmapr_project(
  x,
  region_col,
  file = NULL,
  region_offsets = NULL,
  label_offsets = NULL,
  labels = NULL,
  region_palette = NULL,
  state = NULL,
  hierarchy = NULL,
  styles = NULL,
  label_col = region_col,
  title = NULL,
  ...
)
}
\arguments{
\item{x}{An \code{sf} object in a projected CRS — the source geometry for the
project.}

\item{region_col}{Column in \code{x} defining draggable groups.}

\item{file}{Output path for the ZIP file. Should end in \code{.zip}. When
\code{NULL}, a temporary file is created and its path returned invisibly.}

\item{region_offsets}{Optional data frame with \code{region}, \code{dx_m}, and \code{dy_m}
columns, or a path to such a CSV. When \code{NULL}, all regions are placed at
their original positions.}

\item{label_offsets}{Optional data frame with \code{label_id}, \code{region}, \code{dx_m},
and \code{dy_m} columns, or a path to such a CSV.}

\item{labels}{Optional label table as returned by \code{\link[=make_region_labels]{make_region_labels()}} or
\code{\link[=as_drag_labels]{as_drag_labels()}}.}

\item{region_palette}{Optional named character vector of fill colors (names
are region values, values are hex strings).}

\item{state}{Optional \code{\link[=d_state]{d_state()}} persisted as \code{state.json}. Its
offset/style tables supply defaults when explicit arguments are omitted.}

\item{hierarchy}{Optional \code{\link[=d_hierarchy_state]{d_hierarchy_state()}} persisted as
\code{hierarchy.json}.}

\item{styles}{Optional \code{\link[=d_styles]{d_styles()}} table persisted as \code{styles.csv}.}

\item{label_col}{Column used for default label text. Defaults to
\code{region_col}.}

\item{title}{Optional project title stored in metadata.}

\item{...}{Additional named metadata fields written to \code{metadata.json}.}
}
\value{
Invisibly returns \code{file} (the path to the written ZIP).
}
\description{
Packages a source \code{sf} object together with region offsets, label offsets,
labels, a palette, and metadata into a ZIP file that can be reopened in
Spatial Studio or rendered with \code{\link[=render_dragmapr_project]{render_dragmapr_project()}}. This lets you
create and share reproducible project bundles entirely from R, without
opening the Shiny app.
}
\examples{
if(interactive()){
poly <- sf::st_sf(
  region = c("A", "B"),
  geometry = sf::st_sfc(
    sf::st_polygon(list(rbind(c(0,1e5),c(1e5,1e5),c(1e5,2e5),c(0,2e5),c(0,1e5)))),
    sf::st_polygon(list(rbind(c(0,0),c(1e5,0),c(1e5,1e5),c(0,1e5),c(0,0)))),
    crs = 3857
  )
)
offsets <- data.frame(region = c("A", "B"), dx_m = c(50000, -50000), dy_m = 0)
zip_path <- write_dragmapr_project(
  poly,
  region_col     = "region",
  region_offsets = offsets,
  title          = "Demo project",
  file           = tempfile(fileext = ".zip")
)
file.exists(zip_path)
}
}
\seealso{
\code{\link[=read_dragmapr_project]{read_dragmapr_project()}} to read a project back into R;
\code{\link[=render_dragmapr_project]{render_dragmapr_project()}} to render a project bundle directly.
}
