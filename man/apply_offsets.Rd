% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/offsets.R
\name{apply_offsets}
\alias{apply_offsets}
\title{Apply rigid offsets to grouped sf geometries}
\usage{
apply_offsets(x, offsets, region_col)
}
\arguments{
\item{x}{An \code{sf} object in a projected CRS.}

\item{offsets}{A data frame with \code{region}, \code{dx_m}, and \code{dy_m}, or a CSV path.}

\item{region_col}{Column in \code{x} defining draggable groups.}
}
\value{
An \code{sf} object with geometries translated by group.
}
\description{
Apply rigid offsets to grouped sf geometries
}
\examples{
regions <- sf::st_sf(
  region = c("A", "B"),
  geometry = sf::st_sfc(sf::st_point(c(0, 0)), sf::st_point(c(100, 100)),
                        crs = 3857)
)
offsets <- data.frame(region = "A", dx_m = 5000, dy_m = -2000)
apply_offsets(regions, offsets, region_col = "region")
}
