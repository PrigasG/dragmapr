% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transition.R
\name{make_group_boundaries}
\alias{make_group_boundaries}
\title{Build dotted group-drag boundaries for expanded groups}
\usage{
make_group_boundaries(sf_obj, group_col, padding = NULL)
}
\arguments{
\item{sf_obj}{An \code{sf} object containing the (expanded) child regions.}

\item{group_col}{Column in \code{sf_obj} identifying the expanded group, usually
the parent id column.}

\item{padding}{Single positive number in map units, or \code{NULL} (default) to
use 2.5 percent of the bounding-box diagonal of \code{sf_obj}.}
}
\value{
An \code{sf} object with one row per group and columns \code{group_id},
\code{xmin}, \code{xmax}, \code{ymin}, \code{ymax} (padded bounds), plus the rectangle
geometry.
}
\description{
Computes one padded rectangular boundary per group. The boundary is returned as real geometry with the group's id so
it can be drawn inside the same container as the children and therefore
moves with the group when dragged. In Spatial Studio this boundary is a
drag handle, not a collapse control.
}
\examples{
rect <- function(x0, x1, y0, y1) {
  sf::st_polygon(list(cbind(c(x0, x1, x1, x0, x0), c(y0, y0, y1, y1, y0))))
}
children <- sf::st_sf(
  county = c("A", "A", "B"),
  geometry = sf::st_sfc(
    rect(0, 1, 0, 1), rect(2, 3, 0, 1), rect(5, 6, 0, 1),
    crs = 3857
  )
)
make_group_boundaries(children, group_col = "county")
}
\seealso{
\code{\link[=build_elastic_transition]{build_elastic_transition()}}, which calls this automatically when
\code{group_drag_frame = TRUE}.
}
