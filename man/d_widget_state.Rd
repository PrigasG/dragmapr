% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/widget.R
\name{d_widget_state}
\alias{d_widget_state}
\title{Reconstruct a dragmapr state from a browser state event}
\usage{
d_widget_state(value)
}
\arguments{
\item{value}{The list received from \code{input[[paste0(outputId, "_state")]]}.
\code{NULL} (no event yet) returns \code{NULL}.}
}
\value{
A \code{dragmapr_state}, or \code{NULL} if \code{value} is \code{NULL}.
}
\description{
The native widget reports edits to Shiny through an input named
\code{paste0(outputId, "_state")}. This function turns that value back into a
\code{\link[=d_state]{d_state()}} so server code can persist, merge, or re-render the user's
live composition. It is the inbound half of the widget bridge that
\code{\link[=d_widget]{d_widget()}} / \code{\link[=updateDragmapr]{updateDragmapr()}} form on the outbound side.
}
\details{
The browser's monotonic \code{revision} becomes the state \code{version} unchanged --
the client owns the live counter, so the server records the edit faithfully
rather than bumping it again. The empty-string selection sentinel is mapped
back to \code{NULL}.
}
\examples{
\dontrun{
shiny::observeEvent(input$map_state, {
  state <- d_widget_state(input$map_state)
  write_dragmapr_state(state, "composition.json")
})
}
}
