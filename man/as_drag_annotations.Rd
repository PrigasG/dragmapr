% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/labels.R
\name{as_drag_annotations}
\alias{as_drag_annotations}
\title{Coerce data to draggable annotation boxes}
\usage{
as_drag_annotations(
  labels,
  width_px = 150,
  height_px = 72,
  connector = FALSE,
  connector_type = c("straight", "elbow", "curve", "squiggle")
)
}
\arguments{
\item{labels}{A data frame with \code{label_id}, \code{region}, \code{label}, \code{x}, and \code{y}.}

\item{width_px, height_px}{Default browser box dimensions for rows that do not
already include \code{width_px} or \code{height_px}.}

\item{connector}{Add connector lines from anchors to annotation boxes.}

\item{connector_type}{One of \code{"straight"}, \code{"elbow"}, \code{"curve"}, or
\code{"squiggle"}.}
}
\value{
A normalized label data frame with \code{label_type = "box"}.
}
\description{
Annotation boxes use the same position and state workflow as labels, but
render as larger text boxes for notes, callouts, or location descriptions.
}
\examples{
as_drag_annotations(data.frame(
  label_id = "note-1", region = "North",
  label = "This note can hold more text than a short label.",
  x = 50000, y = 150000
))
}
