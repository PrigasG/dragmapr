% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/widget.R
\name{d_edit}
\alias{d_edit}
\title{Open the interactive dragmapr editor}
\usage{
d_edit(x, region_col = NULL, state = NULL, ...)
}
\arguments{
\item{x}{A projected \code{sf} object, an explodemap \code{grouped_exploded_map}, or a
\code{dragmapr_layout}. Layout objects supply their own draggable geometry and
region column.}

\item{region_col}{Column defining draggable groups. Required when \code{x} is a
raw \code{sf}; inferred from layout objects when omitted.}

\item{state}{Optional \code{\link[=d_state]{d_state()}} giving the initial composition.}

\item{...}{Further arguments passed to \code{\link[=d_widget]{d_widget()}}, for example
\code{label_col}, \code{display_options}, \code{width}, or \code{height}.}
}
\value{
A \code{dragmapr} htmlwidget (the interactive editor).
}
\description{
A friendly front-door to the native draggable editor and the composition step
of the state-first workflow: compute a layout, edit it, render the edited
state. It accepts the objects produced upstream -- a projected \code{sf}, an
explodemap \code{grouped_exploded_map}, or a \code{dragmapr_layout} -- together with an
optional \code{\link[=d_state]{d_state()}}, and returns a configured \code{\link[=d_widget]{d_widget()}}
ready to embed in Shiny, R Markdown, or the viewer.
}
\details{
For an explodemap layout, pass the composed state explicitly so the editor
opens on the exploded composition rather than the bare geometry:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{layout <- explodemap::explode_grouped(x, region_col = "region")
state  <- explodemap::as_dragmapr_state(layout)
d_edit(layout, state = state)
}\if{html}{\out{</div>}}

To capture edits back into a \code{dragmapr_state} (to persist, merge, or
re-render them), read the widget's state input in Shiny with
\code{\link[=d_widget_state]{d_widget_state()}}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{output$map <- renderDragmapr(d_edit(layout, state = state))
observeEvent(input$map_state, \{
  state <- d_widget_state(input$map_state)
\})
}\if{html}{\out{</div>}}
}
\seealso{
\code{\link[=d_widget]{d_widget()}}, \code{\link[=d_widget_state]{d_widget_state()}}.
}
