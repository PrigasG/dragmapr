% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_io.R
\name{d_iframe_bridge}
\alias{d_iframe_bridge}
\title{Build the Shiny iframe bridge JavaScript for a draggable helper}
\usage{
d_iframe_bridge(
  region_input = "region_csv",
  label_input = "label_csv",
  slow_poll_ms = 2000L,
  fast_poll_ms = 500L,
  allowed_origin = "same-origin",
  iframe_selector = "iframe"
)
}
\arguments{
\item{region_input}{Name of the Shiny input that receives the region-offset
CSV text.  Defaults to \code{"region_csv"}.}

\item{label_input}{Name of the Shiny input that receives the label-offset
CSV text.  Defaults to \code{"label_csv"}.}

\item{slow_poll_ms}{Polling interval in milliseconds once initial state has
been received.  Defaults to \code{2000}.}

\item{fast_poll_ms}{Polling interval in milliseconds before initial state
arrives.  Defaults to \code{500}.}

\item{allowed_origin}{Origin allowed to send drag state messages. Use
\code{"same-origin"} to accept the current Shiny app origin, \code{"*"} to accept
any origin, or a specific origin such as \code{"https://example.com"}.}

\item{iframe_selector}{CSS selector used to find the drag-map helper iframe
in the parent document.  Defaults to \code{"iframe"} (first iframe found), but
a more specific selector such as \code{"#my-helper-frame"} or
\code{"iframe.studio-helper-frame"} is recommended when the page may contain
other iframes (e.g. Shiny download handlers).}
}
\value{
A character string of JavaScript ready for
\code{tags$head(tags$script(HTML(d_iframe_bridge())))}.
}
\description{
Returns the JavaScript needed to pass region and label offset state from the
draggable helper iframe back into Shiny inputs via \code{postMessage}, and to
poll the iframe until state arrives.  The string is suitable for wrapping
in \code{tags$head(tags$script(HTML(...)))}.
}
\examples{
# In a Shiny UI:
if(interactive()){
library(shiny)
ui <- fluidPage(
  tags$head(tags$script(HTML(
    d_iframe_bridge(iframe_selector = "#my-helper-frame")
  ))),
  uiOutput("helper")  # render tags$iframe(id = "my-helper-frame", ...)
)
}
}
