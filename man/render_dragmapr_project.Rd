% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project.R
\name{render_dragmapr_project}
\alias{render_dragmapr_project}
\title{Render a static map from a Spatial Studio project bundle}
\usage{
render_dragmapr_project(
  project,
  file = NULL,
  width = 10,
  height = 8,
  dpi = 300,
  title = NULL,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{project}{Path to a \code{dragmapr-project.zip} file or an extracted project
directory created by Spatial Studio.}

\item{file}{Optional output image path. When supplied, the rendered plot is
saved with \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}

\item{width, height, dpi}{Output settings used when \code{file} is supplied.}

\item{title}{Optional plot title. Defaults to the title saved in project
metadata, when available.}

\item{quiet}{Use \code{TRUE} to suppress validation messages about zero-filled or
extra offset rows.}

\item{...}{Additional arguments passed to \code{\link[=render_dragged_map]{render_dragged_map()}}, such as
\code{legend_position}, \code{show_legend}, or \code{label_marker_shape}.}
}
\value{
A \code{ggplot} object.
}
\description{
\code{render_dragmapr_project()} turns a Spatial Studio project ZIP, or an
extracted project directory, into the same \code{ggplot2} image produced by
\code{\link[=render_dragged_map]{render_dragged_map()}}. It reads the saved source geometry, region offsets,
label offsets, label table, palette, and metadata, then validates that the
pieces still match before rendering.
}
\examples{
\dontrun{
render_dragmapr_project("dragmapr-project.zip", file = "map.png")
}
}
