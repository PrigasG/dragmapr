% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/labels.R
\name{as_drag_labels}
\alias{as_drag_labels}
\title{Coerce data to a drag label table}
\usage{
as_drag_labels(labels)
}
\arguments{
\item{labels}{A data frame with \code{label_id}, \code{region}, \code{label}, \code{x}, and \code{y}.
Optional columns include \code{label_type} (\code{"label"} or \code{"box"}),
\code{width_px} and \code{height_px} for draggable info boxes, and \code{connector},
\code{connector_type}, \code{connector_start_x}, \code{connector_start_y},
\code{connector_mid_x}, and \code{connector_mid_y} for leader lines.}
}
\value{
A normalized label data frame.
}
\description{
Use this for user-supplied labels. Extra columns are preserved so interactive
applications can carry styling, tooltip, or grouping metadata alongside the
core label position columns.
}
\examples{
as_drag_labels(data.frame(
  label_id = "note-1", region = "North", label = "Check this",
  x = 50000, y = 150000, tooltip = "extra metadata"
))
}
