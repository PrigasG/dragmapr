% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{d_diagnostics}
\alias{d_diagnostics}
\title{Summarise a spatial dataset for use with dragmapr}
\usage{
d_diagnostics(x, region_col = NULL, quiet = FALSE)
}
\arguments{
\item{x}{An \code{sf} object.}

\item{region_col}{Optional column name. When supplied, hierarchy details are
reported relative to that column and it is highlighted in the summary.}

\item{quiet}{Logical. When \code{TRUE}, the summary is not printed and the result
list is returned invisibly without any output.}
}
\value{
An invisible named list with components:
\describe{
\item{\code{geometry_type}}{Character vector of unique geometry type names.}
\item{\code{crs}}{CRS identifier string (e.g. \code{"EPSG:3857"} or
\code{"Unknown CRS"}).}
\item{\code{is_longlat}}{Logical — \code{TRUE} when the CRS is geographic.}
\item{\code{n_features}}{Integer row count.}
\item{\code{candidate_cols}}{Character vector of candidate grouping columns.}
\item{\code{column_details}}{Named list with \code{n_unique} and \code{n_empty} per
candidate column.}
\item{\code{n_invalid_geometries}}{Number of features with invalid geometry.}
\item{\code{hierarchy_pairs}}{List of detected parent-to-child column pairs,
each with \code{parent}, \code{child}, \code{n_parent}, \code{n_child}, and
\code{child_repeats_across_parents}.}
\item{\code{recommended_path}}{Suggested grouping path string, or \code{NULL}.}
}
}
\description{
Inspects an \code{sf} object and reports geometry type, coordinate reference
system, feature count, candidate grouping columns (including empty-value
counts), detected parent-to-child column hierarchies (including cases where
child names repeat across parents), and invalid geometry counts. The output
guides column selection in \code{\link[=drag_map_prototype]{drag_map_prototype()}} and helps diagnose
unexpected behaviour when uploading custom spatial files to Spatial Studio.
}
\examples{
poly <- sf::st_sf(
  county = c("Essex", "Essex", "Morris", "Morris"),
  mun    = c("Fairfield", "Caldwell", "Fairfield", "Roxbury"),
  geometry = sf::st_sfc(
    sf::st_polygon(list(rbind(c(0,1e5),c(1e5,1e5),c(1e5,2e5),c(0,2e5),c(0,1e5)))),
    sf::st_polygon(list(rbind(c(0,0),c(1e5,0),c(1e5,1e5),c(0,1e5),c(0,0)))),
    sf::st_polygon(list(rbind(c(1e5,1e5),c(2e5,1e5),c(2e5,2e5),c(1e5,2e5),c(1e5,1e5)))),
    sf::st_polygon(list(rbind(c(1e5,0),c(2e5,0),c(2e5,1e5),c(1e5,1e5),c(1e5,0)))),
    crs = 3857
  )
)
diag <- d_diagnostics(poly)
}
