% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state.R
\name{dragmapr_state}
\alias{dragmapr_state}
\title{Create a dragmapr state object}
\usage{
dragmapr_state(
  level = "region",
  region_col = NULL,
  label_id_col = NULL,
  region_offsets = NULL,
  label_offsets = NULL,
  expanded_groups = character(),
  view = NULL,
  version = 0L,
  crs = NULL,
  geometry_id = NULL,
  selected_feature = NULL,
  binding = NULL,
  schema_version = "1.1.0",
  package_version = as.character(utils::packageVersion("dragmapr"))
)
}
\arguments{
\item{level}{Character label for the active geography level.}

\item{region_col}{Source-geometry column used to join \code{region_offsets$region}
back to features. Defaults to \code{level} for old/simple states, but should be
set explicitly for saved projects and package handoffs.}

\item{label_id_col}{Source or label-table column identifying draggable
labels.}

\item{region_offsets}{Data frame with \code{region}, \code{dx_m}, and \code{dy_m}.}

\item{label_offsets}{Data frame with \code{label_id}, \code{region}, \code{dx_m}, and
\code{dy_m}.}

\item{expanded_groups}{Character vector of expanded parent groups.}

\item{view}{Optional list describing the client view, such as scale and
translation.}

\item{version}{Integer state revision.}

\item{crs}{Optional coordinate reference system the metre offsets are
expressed in. Accepts anything \code{sf::st_crs()} understands (an EPSG code,
a PROJ/WKT string, or a \code{crs} object). Stored as an EPSG integer when
available, otherwise as a WKT string, so it round-trips through JSON.
Because \code{dx_m}/\code{dy_m} are only meaningful in a projected CRS, recording it
here makes a saved state safe to reapply in a later session.}

\item{geometry_id}{Optional single string identifying the geometry this
state was composed against (provenance / binding tag). Lets a downstream
consumer detect when a state is being applied to a different dataset than
it was authored for.}

\item{selected_feature}{Optional single string naming the feature currently
selected in a dashboard or editor. Carried so a composition can be
restored with focus intact.}

\item{binding}{Optional list of binding metadata. When supplied,
\code{binding$region_col} and \code{binding$label_id_col} are used as defaults for
the corresponding top-level arguments.}

\item{schema_version}{State schema version. Stored with JSON snapshots so
future migrations can read older projects.}

\item{package_version}{Package version that created the state.}
}
\value{
An object of class \code{"dragmapr_state"}.
}
\description{
A \code{dragmapr_state} stores the mutable, editorial layout of a draggable map
separately from the source geometry and from display options. It is the
shared composition contract handed between layout producers (such as
\code{explodemap::as_dragmapr_state()}), the interactive editor, and static
renderers.
}
\details{
The state is deliberately geometry-free: it carries \emph{deltas}, not absolute
geometry, and is bound to features at apply time via a join key. That key is
recorded in \code{region_col}, while \code{level} remains the human geography level
label. For durable round-trips prefer a stable code (e.g. a FIPS/ISO id)
over a display name, and record which geometry the state was composed
against with \code{geometry_id}.
}
