% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render.R
\name{render_dragged_map}
\alias{render_dragged_map}
\title{Render an adjusted draggable plot with labels}
\usage{
render_dragged_map(
  x,
  region_offsets = NULL,
  region_col,
  label_col = region_col,
  label_offsets = NULL,
  labels = NULL,
  label_values = NULL,
  region_palette = NULL,
  region_labels = NULL,
  show_legend = TRUE,
  max_legend_keys = 25L,
  legend_position = c("bottom", "top", "left", "right", "none"),
  legend_title = "Region",
  legend_values = NULL,
  title = NULL,
  label_size = 3.4,
  show_label_marker = TRUE,
  label_marker_shape = c("circle", "rect", "none"),
  marker_size = 3.2,
  marker_fill = "white",
  marker_color = "black",
  marker_stroke = 0.8,
  box_label_size = 3,
  box_fill = "white",
  box_color = "black",
  box_wrap = 24,
  connector_color = "#334155",
  connector_linewidth = 0.35,
  connector_linetype = "solid",
  connector_endpoint = c("none", "arrow"),
  connector_curvature = 0.18,
  connector_squiggle_amplitude = 12000,
  connector_squiggle_waves = 4,
  connector_end_gap = NULL,
  show_origin_outlines = FALSE,
  show_movement_connectors = FALSE,
  show_movement_band = FALSE,
  movement_connector_color = "#64748b",
  movement_connector_opacity = 0.72,
  movement_connector_linewidth = 0.45,
  movement_connector_linetype = c("solid", "dashed", "dotted"),
  movement_connector_endpoint = c("closed", "open", "none"),
  label_padding = 0.08,
  map_background = c("white", "transparent", "light_grid", "dark"),
  file = NULL,
  width = 8,
  height = 6,
  dpi = 300
)
}
\arguments{
\item{x}{An \code{sf} object in a projected CRS.}

\item{region_offsets}{Region offsets as a data frame, CSV path, or \code{NULL}.}

\item{region_col}{Column defining draggable groups.}

\item{label_col}{Column used for default region-label text. Defaults to
\code{region_col}.}

\item{label_offsets}{Label state as a data frame, CSV path, or \code{NULL}.}

\item{labels}{Optional label table from \code{make_region_labels()} or
\code{as_drag_labels()}. Use \code{FALSE} to omit labels from the static render.}

\item{label_values}{Optional character vector of label IDs to render.
\code{NULL} renders all labels.}

\item{region_palette}{Optional named vector of fill colors keyed by region.}

\item{region_labels}{Optional named vector of legend labels keyed by region.}

\item{show_legend}{Show the region fill legend.  When the number of distinct
regions exceeds \code{max_legend_keys}, the legend is suppressed regardless of
this setting (with an informational message).}

\item{max_legend_keys}{Maximum number of legend keys before the legend is
automatically hidden.  Defaults to \code{25}.  Set \code{Inf} to always show.}

\item{legend_position}{Position of the fill legend in static exports.
One of \code{"bottom"}, \code{"top"}, \code{"left"}, \code{"right"}, or \code{"none"}.}

\item{legend_title}{Title shown above the fill legend. Defaults to
\code{"Region"}.}

\item{legend_values}{Optional character vector of region values to include
in the legend. \code{NULL} includes all region values.}

\item{title}{Optional plot title.}

\item{label_size}{Text size passed to \code{\link[ggplot2:geom_text]{ggplot2::geom_text()}}. Defaults to
\code{3.4}.}

\item{show_label_marker}{Draw markers behind ordinary text labels. Use
\code{FALSE} for text-only labels.}

\item{label_marker_shape}{Marker shape for ordinary text labels in static
exports. One of \code{"circle"}, \code{"rect"}, or \code{"none"}. If
\code{show_label_marker = FALSE}, this is treated as \code{"none"}.}

\item{marker_size}{Point size passed to \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}}. Defaults to
\code{3.2}.}

\item{marker_fill}{Fill colour of the label marker circle. Defaults to
\code{"white"}.}

\item{marker_color}{Stroke colour of the label marker circle. Defaults to
\code{"black"}.}

\item{marker_stroke}{Stroke width of the label marker circle. Defaults to
\code{0.8}.}

\item{box_label_size}{Text size for annotation boxes. Defaults to \code{3}.}

\item{box_fill, box_color}{Fill and border colours for annotation boxes.}

\item{box_wrap}{Approximate character width used to wrap annotation-box text.}

\item{connector_color, connector_linewidth, connector_linetype}{Static export
styling for label connector lines.}

\item{connector_endpoint}{Connector endpoint style. One of \code{"none"} or
\code{"arrow"}.}

\item{connector_curvature}{Curvature used by \code{"curve"} connectors.}

\item{connector_squiggle_amplitude, connector_squiggle_waves}{Appearance of
\code{"squiggle"} connectors in static exports.
\code{connector_squiggle_amplitude} is in the same coordinate units as the
projected CRS. For common metre-based CRSs such as EPSG:3857, the value is
metres; for degree-based or custom projected CRSs, use that CRS's units.
The default of \code{12000} is tuned for country-scale metre-based datasets;
scale it up or down to match your data extent.}

\item{connector_end_gap}{Distance, in plot units, to trim connector endpoints
away from label centers. Defaults to an automatic value based on plot size.}

\item{show_origin_outlines}{Show the original, unshifted outlines of regions
with non-zero offsets beneath the moved regions.}

\item{show_movement_connectors}{Draw a connector from each moved region's
original representative point to its translated location.}

\item{show_movement_band}{Draw a swept shadow between each region's original
footprint and its translated position. The shadow is built from the actual
polygon boundary, so it follows the shape of the region rather than a flat
bounding-box band.}

\item{movement_connector_color, movement_connector_opacity, movement_connector_linewidth}{Static export styling for movement connectors and the swept movement shadow.}

\item{movement_connector_linetype}{Movement connector line style. One of
\code{"solid"}, \code{"dashed"}, or \code{"dotted"}.}

\item{movement_connector_endpoint}{Movement connector endpoint. One of
\code{"none"}, \code{"open"}, or \code{"closed"}.}

\item{label_padding}{Proportional padding added around displaced labels and
connectors to avoid clipping static exports.}

\item{map_background}{Static map background. One of \code{"white"},
\code{"transparent"}, \code{"light_grid"}, or \code{"dark"}.}

\item{file}{Optional output image path. When supplied, \code{ggplot2::ggsave()}
saves the plot.}

\item{width, height, dpi}{Output settings used when \code{file} is supplied.}
}
\value{
A \code{ggplot} object.
}
\description{
Render an adjusted draggable plot with labels
}
\examples{
# render_dragged_map() is an optional static-export step.
# The primary workflow is drag_map_prototype(open = TRUE): open the
# interactive HTML, drag regions and labels to your liking, then
# copy or download the offset CSVs.  Only call render_dragged_map()
# if you also need a reproducible ggplot2 image from those offsets.
if(interactive()){
# Step 1 (always): open the interactive draggable plot.
drag_map_prototype(poly, region_col = "region", open = TRUE)

# Step 2 (optional): after dragging, reconstruct as a static image.
region_offsets <- read_offsets("my_region_offsets.csv")
render_dragged_map(poly, region_col = "region",
                  region_offsets = region_offsets)
}

# Minimal runnable example (no prior dragging session needed).
poly <- sf::st_sf(
  region = c("A", "B"),
  geometry = sf::st_sfc(
    sf::st_polygon(list(rbind(c(0,1e5),c(1e5,1e5),c(1e5,2e5),c(0,2e5),c(0,1e5)))),
    sf::st_polygon(list(rbind(c(0,0),c(1e5,0),c(1e5,1e5),c(0,1e5),c(0,0)))),
    crs = 3857
  )
)
render_dragged_map(poly, region_col = "region")
}
\seealso{
\code{\link[=drag_map_prototype]{drag_map_prototype()}} for the interactive draggable browser helper
that produces the offset CSVs; \code{\link[=read_offsets]{read_offsets()}} and \code{\link[=read_label_state]{read_label_state()}}
to load saved offsets from disk; \code{\link[=as_drag_annotations]{as_drag_annotations()}} for info-box labels.
}
