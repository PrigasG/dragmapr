% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchy.R
\name{create_layout_snapshot}
\alias{create_layout_snapshot}
\title{Save a layout snapshot for a grouped sf dataset}
\usage{
create_layout_snapshot(x, group, offsets = NULL)
}
\arguments{
\item{x}{A data frame or \code{sf} object.}

\item{group}{Column name (scalar character) defining the current grouping.}

\item{offsets}{A data frame with \code{region}, \code{dx_m}, and \code{dy_m} columns, a
CSV path to such a file, or \code{NULL} for an all-zero starting state.}
}
\value{
A named list with elements \code{group} (character), \code{offsets} (data
frame or \code{NULL}), and \code{timestamp} (POSIXct).
}
\description{
Captures the current drag offsets for a given grouping and returns a named
list. Snapshots can be passed to \code{\link[=inherit_layout]{inherit_layout()}} when switching to a
finer grouping, or stored and later recovered with
\code{\link[=restore_layout_snapshot]{restore_layout_snapshot()}}.
}
\examples{
poly <- sf::st_sf(
  county = c("A", "B"),
  geometry = sf::st_sfc(
    sf::st_polygon(list(rbind(c(0,1e5),c(1e5,1e5),c(1e5,2e5),c(0,2e5),c(0,1e5)))),
    sf::st_polygon(list(rbind(c(0,0),c(1e5,0),c(1e5,1e5),c(0,1e5),c(0,0)))),
    crs = 3857
  )
)
offsets <- data.frame(region = c("A", "B"), dx_m = c(10000, -10000), dy_m = 0)
snap <- create_layout_snapshot(poly, group = "county", offsets = offsets)
snap$group
snap$offsets
}
\seealso{
\code{\link[=restore_layout_snapshot]{restore_layout_snapshot()}}, \code{\link[=inherit_layout]{inherit_layout()}}.
}
