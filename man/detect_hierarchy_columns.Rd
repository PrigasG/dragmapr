% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hierarchy.R
\name{detect_hierarchy_columns}
\alias{detect_hierarchy_columns}
\title{Detect parent-child grouping columns}
\usage{
detect_hierarchy_columns(
  x,
  cols = NULL,
  max_child_groups = 600L,
  min_confidence = 0.72
)
}
\arguments{
\item{x}{A data frame or \code{sf} object.}

\item{cols}{Optional character vector of columns to inspect. \code{NULL} uses
non-geometry atomic columns.}

\item{max_child_groups}{Maximum number of child groups to recommend for
interactive bloom. Pairs above this limit are returned but flagged.}

\item{min_confidence}{Minimum confidence for the \code{recommended} flag.}
}
\value{
A data frame with one row per detected pair.
}
\description{
Finds candidate hierarchy pairs in a data frame or \code{sf} object. A pair is
treated as parent-child when the child column has more groups than the
parent column and most child values sit inside one parent group.
}
\examples{
df <- data.frame(
  county = c("A", "A", "B", "B"),
  town = c("A1", "A2", "B1", "B2")
)
detect_hierarchy_columns(df)
}
