% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transition.R
\name{build_elastic_transition}
\alias{build_elastic_transition}
\title{Build a local elastic parent-to-child transition}
\usage{
build_elastic_transition(
  child_sf,
  parent_sf,
  parent_col,
  parent_id_col = parent_col,
  options = transition_options()
)
}
\arguments{
\item{child_sf}{An \code{sf} object with the child regions in their final
(expanded) positions.}

\item{parent_sf}{An \code{sf} object with the parent regions.}

\item{parent_col}{Column in \code{child_sf} holding each child's parent id.}

\item{parent_id_col}{Column in \code{parent_sf} holding the parent id. Defaults
to \code{parent_col}, which covers the common case where both layers share the
same column name (e.g. \code{"COUNTY"}).}

\item{options}{A list created by \code{\link[=transition_options]{transition_options()}}.}
}
\value{
A list of class \code{"dragmapr_transition"} with elements:
\describe{
\item{\code{anchored}}{\code{child_sf} plus \code{anchor_x}, \code{anchor_y}, \code{final_x},
\code{final_y}, \code{move_dist}, \code{move_ratio}, \code{stretch_strength}, and
\code{duration_ms} columns.}
\item{\code{frames}}{An \code{sf} object of \code{n_frames} stacked copies of the
children with \code{frame_id}, \code{frame_progress}, and \code{frame_eased} columns
and interpolated geometry, suitable for \code{ggplot2} playback or export.}
\item{\code{boundaries}}{The dotted group-drag boundary per parent group
from \code{\link[=make_group_boundaries]{make_group_boundaries()}}, or \code{NULL} when
\code{options$group_drag_frame} is \code{FALSE}.}
\item{\code{options}}{The validated options used.}
}
}
\description{
Prepares everything needed to animate child regions blooming out of their
parent region: per-child anchor and final positions, relative motion
strengths, ready-to-plot animation frames, and the dotted group-drag
boundary for each expanded group. The parent layout itself is never moved
(stable skeleton); only the expanded branch animates.
}
\details{
Children whose parent id has no match in \code{parent_sf} fall back to their own
centroid as the anchor (they fade in place instead of blooming), and a
warning lists the unmatched ids so data problems are visible early.

The returned \code{frames} interpolate each child from a small seed at the
parent centroid (frame 1) to its exact final geometry (last frame) using an
ease-out-back curve, so the last frame equals \code{child_sf} and can be used as
the settled state.
}
\examples{
rect <- function(x0, x1, y0, y1) {
  sf::st_polygon(list(cbind(c(x0, x1, x1, x0, x0), c(y0, y0, y1, y1, y0))))
}
parents <- sf::st_sf(
  county = c("A", "B"),
  geometry = sf::st_sfc(rect(0, 3, 0, 3), rect(4, 7, 0, 3), crs = 3857)
)
children <- sf::st_sf(
  county = c("A", "A", "B", "B"),
  mun    = c("A1", "A2", "B1", "B2"),
  geometry = sf::st_sfc(
    rect(-1, 1, 0, 3), rect(2, 4, 0, 3),
    rect(3, 5, 0, 3), rect(6, 8, 0, 3),
    crs = 3857
  )
)
tr <- build_elastic_transition(
  children, parents,
  parent_col = "county",
  options    = transition_options(n_frames = 5)
)
names(tr)
head(tr$anchored[, c("county", "mun", "anchor_x", "final_x")])
tr$boundaries
}
\seealso{
\code{\link[=transition_options]{transition_options()}}, \code{\link[=make_group_boundaries]{make_group_boundaries()}},
\code{\link[=layout_metrics]{layout_metrics()}}, \code{\link[=inherit_layout]{inherit_layout()}} for carrying drag offsets from
parent to child groupings.
}
