% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_io.R
\name{read_dragmapr_sf_url}
\alias{read_dragmapr_sf_url}
\title{Download and read an sf object from a URL}
\usage{
read_dragmapr_sf_url(url, timeout = 60)
}
\arguments{
\item{url}{A non-empty character string pointing to a downloadable spatial
file.}

\item{timeout}{Download timeout in seconds.  Defaults to \code{60}.}
}
\value{
An \code{sf} object.
}
\description{
Downloads a spatial file from \code{url} into a temporary directory and reads it
with \code{\link[sf:st_read]{sf::st_read()}}.  Supported direct formats are \code{.geojson}, \code{.json},
and \code{.gpkg}.  A \code{.zip} URL is extracted first and the first supported file
inside is read.  For ambiguous extensions the function assumes a zip archive.
}
\examples{
if(interactive()){
x <- read_dragmapr_sf_url("https://example.com/regions.geojson")
}
}
