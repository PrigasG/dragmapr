% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_io.R
\name{prepare_dragmapr_sf}
\alias{prepare_dragmapr_sf}
\title{Prepare an sf object for use with dragmapr}
\usage{
prepare_dragmapr_sf(x, target_crs = 3857)
}
\arguments{
\item{x}{An \code{sf} object.}

\item{target_crs}{EPSG code for the projected CRS to use when the input is
geographic.  Defaults to \code{3857} (Web Mercator).}
}
\value{
A projected \code{sf} object containing only polygon or multipolygon
features with valid geometry.
}
\description{
Repairs invalid geometry, filters to polygon types, assigns a fallback CRS
when none is present, and reprojects geographic (longitude/latitude) data to
a projected CRS so metre offsets work correctly.
}
\examples{
poly <- sf::st_sf(
  region = "A",
  geometry = sf::st_sfc(
    sf::st_polygon(list(rbind(c(-1,0),c(1,0),c(1,1),c(-1,1),c(-1,0)))),
    crs = 4326
  )
)
out <- prepare_dragmapr_sf(poly)
sf::st_is_longlat(out)  # FALSE
}
