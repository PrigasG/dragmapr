% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_io.R
\name{read_dragmapr_sf_upload}
\alias{read_dragmapr_sf_upload}
\title{Read an sf object from a Shiny file upload}
\usage{
read_dragmapr_sf_upload(upload)
}
\arguments{
\item{upload}{The value of \verb{input$<id>} from a \code{\link[shiny:fileInput]{shiny::fileInput()}} widget.
Each row is one uploaded file with \code{name} and \code{datapath} columns.  When
\code{NULL} or a zero-row data frame, returns \code{NULL}.}
}
\value{
An \code{sf} object, or \code{NULL} if \code{upload} is \code{NULL} or empty.
}
\description{
Handles the data frame returned by \code{\link[shiny:fileInput]{shiny::fileInput()}}, including
multi-file uploads of shapefile sidecars (\code{.shp}, \code{.dbf}, \code{.shx}, \code{.prj})
and zipped archives containing any supported format.  When \code{upload} is
\code{NULL} or empty the function returns \code{NULL} so callers can fall back to
demo data.
}
\examples{
# Typical Shiny server usage:
if(interactive()){
observeEvent(input$spatial_upload, {
  x <- read_dragmapr_sf_upload(input$spatial_upload)
  if (!is.null(x)) {
    state$source <- x
  }
})
}
}
