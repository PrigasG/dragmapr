% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transition.R
\name{layout_metrics}
\alias{layout_metrics}
\title{Measure mental-map stability between two layouts}
\usage{
layout_metrics(before, after, id_col)
}
\arguments{
\item{before}{An \code{sf} object with the layout before the transition.}

\item{after}{An \code{sf} object with the layout after the transition.}

\item{id_col}{Column present in both objects identifying each region.}
}
\value{
A named list with \code{mean_drift} and \code{max_drift} (map units),
\code{stability} (0-100; 100 means no drift, 0 means mean drift of one third
of the \code{before} bounding-box diagonal or more), and \code{n_matched} (regions
compared). Regions missing from \code{after} are dropped with a warning.
}
\description{
Compares region centroids before and after a transition and reports how far
regions drifted. Use it to confirm that expanding one branch left the rest
of the map stable: pass the non-expanded (sibling) regions as \code{before} and
\code{after}. Lower drift means the user's mental map survived.
}
\examples{
rect <- function(x0, x1, y0, y1) {
  sf::st_polygon(list(cbind(c(x0, x1, x1, x0, x0), c(y0, y0, y1, y1, y0))))
}
before <- sf::st_sf(
  region = c("A", "B"),
  geometry = sf::st_sfc(rect(0, 1, 0, 1), rect(2, 3, 0, 1), crs = 3857)
)
after <- before
sf::st_geometry(after)[2] <- sf::st_geometry(after)[[2]] + c(0.5, 0)
layout_metrics(before, after, id_col = "region")
}
\seealso{
\code{\link[=build_elastic_transition]{build_elastic_transition()}}.
}
