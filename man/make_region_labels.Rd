% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/labels.R
\name{make_region_labels}
\alias{make_region_labels}
\title{Derive one default label per draggable region}
\usage{
make_region_labels(
  x,
  region_col,
  label_col = region_col,
  point = c("point_on_surface", "centroid")
)
}
\arguments{
\item{x}{An \code{sf} object in a projected CRS.}

\item{region_col}{Column defining draggable groups.}

\item{label_col}{Column used for label text. Defaults to \code{region_col}.}

\item{point}{One of \code{"point_on_surface"} or \code{"centroid"}.}
}
\value{
A drag label table with \code{label_id}, \code{region}, \code{label}, \code{x}, and \code{y}.
}
\description{
Derive one default label per draggable region
}
\examples{
poly <- sf::st_sf(
  region = c("North", "South"),
  geometry = sf::st_sfc(
    sf::st_polygon(list(rbind(c(0,1e5),c(1e5,1e5),c(1e5,2e5),c(0,2e5),c(0,1e5)))),
    sf::st_polygon(list(rbind(c(0,0),c(1e5,0),c(1e5,1e5),c(0,1e5),c(0,0)))),
    crs = 3857
  )
)
make_region_labels(poly, region_col = "region")
}
